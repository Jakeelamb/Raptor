# NOTES

## Chronological Notes

- 2026-05-20 Raptor was restored to `rescue/gpu-trinity`; current head includes OpenCL rescue smoke and CPU-vs-OpenCL k-mer baseline.
- 2026-05-20 Current evidence proves GPU k-mer acceleration and general test health, not Trinity replacement parity.
- 2026-05-20 Trinity parity requires explicit normalization, Inchworm, Chrysalis, Butterfly, paired-end evidence, output/evaluation, and end-to-end workflow gates.
- 2026-05-20 Official Trinity docs describe normalization, Inchworm, Chrysalis, Butterfly, paired-end/strand-specific handling, optional Jaccard clipping, staged execution, and final transcript outputs. Raptor currently has pieces for several of these, but no frozen Trinity-vs-Raptor biological benchmark.
- 2026-05-20 Initial normalization risk was single-codepath drift: `raptor normalize`, `normalize_reads`, and `normalize_paired_reads` did not present one parameterized implementation.
- 2026-05-20 First tiny alternative-isoform harness is `bench/trinity_parity/run_tiny_fixture.py`. Current Raptor result is deliberately recorded as a failing proxy: 51 short contigs, N50 27, while truth transcripts are 252 and 240 bp.
- 2026-05-20 Bounded read-overlap rescue plus a non-repetitive tiny fixture moved the fast benchmark to 2 contigs, N50 252, mean best truth coverage 1.0, and min best truth coverage 1.0. This is progress, but the next proof needs Trinity/oracle comparison, paired-end evidence, harder fixtures, and public datasets.
- 2026-05-20 Tiny frozen oracle is checked in at `bench/trinity_parity/oracles/tiny_alt_isoform.fa`. `run_tiny_fixture.py` now gates generated-truth and oracle recovery separately at default minimum best coverage 0.95.
- 2026-05-20 `raptor assemble` now accepts `--input2` for paired-end reads. Tiny harness insert sweep 110,140,160,180 passes truth/oracle min coverage 1.0 for every insert. Current implementation uses R1 plus reverse-complemented R2 as sequence evidence; it is not yet full Trinity/Butterfly-style paired path support.
- 2026-05-20 `run_tiny_fixture.py` can now run Trinity on the same paired-end FASTQs with `--run-trinity --require-trinity` and can refresh the tiny oracle with `--freeze-trinity-oracle`. Trinity was not on this machine's PATH during the harness update, so this is a capture path, not a captured live Trinity oracle yet.
- 2026-05-20 Normalization drift was narrowed: `NormalizeConfig` now owns k, target coverage, min abundance, max read/pair limit, and GPU request state for both single and paired paths. `raptor normalize` and the standalone normalizer binaries call this pipeline instead of carrying separate filtering loops/defaults.
- 2026-05-20 The tiny parity harness can run `raptor normalize` before assembly with `--normalize-raptor --assemble-normalized`. On the default insert 160 fixture, normalization kept 15/15 pairs, paired counts matched, and normalized assembly still emitted [252,240] with truth/oracle min coverage 1.0.
- 2026-05-20 `raptor trinity` now runs a minimal normalize -> assemble de novo workflow and writes `raptor_trinity_report.json`. The tiny harness can exercise it with `--run-raptor-workflow --skip-raptor`; default fixture output is [252,240] with truth/oracle min coverage 1.0.
- 2026-05-20 `raptor trinity` now emits `raptor_components.json`, `raptor_component_graphs.json`, `raptor_component_paths.fasta`, `raptor_component_selected_isoforms.fasta`, `raptor_component_selected_isoforms.json`, and `raptor_component_isoform_candidates.json` using `sequence_or_read_kmer` component clustering. The tiny workflow fixture reports 1 component containing contigs [0,1] with 492 total bases, 30 assigned reads, 15 assigned pairs, a 2-node/1-edge component graph with a 91 bp edge supported by 10 reads, 10 pairs, 133 observed shared k-mers, a read-derived k=25 graph with 622 serialized k-mer node records, 622 serialized k-mer edge records, 8 non-branching graph paths covering 622 edges, component transcript candidates with truth/oracle min coverage 1.0, and production selected component isoforms [252,240] with truth/oracle min coverage 1.0 and precision/recall/F1 1.0. The selected isoform evidence JSON has 2 records using `component_contig_evidence_score_v2`; insert-sweep ranks are dense and scores descending with total scores [258,288,221,233], max scores [138,150,115,127], reads [50,55,40,42], pairs [30,35,25,27], read-kmer path counts [12,12,12,12], and max path support [6,6,5,5]. Candidate JSON proves 10 scored candidates, 2 selected contigs, and 8 rejected read-kmer path fragments for every insert in [110,140,160,180]. This starts the read/k-mer-backed Chrysalis/Butterfly graph artifact surface but candidate scoring still needs fusion stress and Trinity comparison.
- 2026-05-20 The parity harness now supports `--fixture ambiguous_paralog`. At insert 160, truth transcript lengths are [240,234,240]; `raptor trinity` emits selected lengths [240,234,240] with truth min coverage 1.0, selected precision 1.0, recall 1.0, F1 1.0, 0 false positives, 0 false negatives, 1 component, 5 component graph edges, 20 isoform candidates, 3 selected contig candidates, and 17 rejected candidates including 16 rejected read-kmer path candidates. This proves a harder ambiguous isoform/paralog case now runs and suppresses the prior partial false-positive contig.
- 2026-05-20 The parity harness now supports failing `--fixture compact_fusion`. At insert 160, truth transcript lengths are [216,216], but `raptor trinity` emits one selected 336 bp fused contig. Truth min coverage is 1.0, but selected precision/recall/F1 are 0.0 with 1 false positive and 2 false negatives. This exposes the next paired-path/fusion-control blocker.
