pub fn assemble_reads(input_path: &str, output_path: &str, min_len: usize, output_gfa: bool, output_gfa2: bool, adaptive_k: bool, use_rle: bool, collapse_repeats: bool, min_repeat_len: usize, polish: bool, polish_window: usize, streaming: bool, export_metadata: bool, json_metadata: Option<String>, tsv_metadata: Option<String>, isoforms: bool, gtf_path: Option<String>, max_path_depth: usize, min_confidence: f64,) { assemble_reads_old(input_path, output_path, min_len, output_gfa, output_gfa2, adaptive_k, use_rle, collapse_repeats, min_repeat_len, polish, polish_window, streaming, export_metadata, json_metadata, tsv_metadata, isoforms); }
